---
description: SecondOp agent workflow, safety gates, and source-of-truth docs
alwaysApply: true
---

# SecondOp Workflow

Linear issue before any code: create product work in **Backlog**; a human moves it to **Todo**. Do not start if the issue is unassigned or In Progress for someone else.

This git repo’s `AGENTS.md` is canonical. An unversioned `SecondOP-Agentic` umbrella folder is overlay-only.

Follow:

- `AGENTS.md` in this repo — Linear/GitHub workflow, software factory, safety rules
- `docs/AI_CONTRACT.md` — required LLM output shape and medical safety rules
- `docs/DEPLOYMENT_RUNBOOK.md` — Vercel + Railway deploy sequence
- `docs/MULTI_AGENT_WORKFLOW.md` — agent roles and handoffs

On-demand workflows live in `.cursor/skills/` (Linear spec, feature delivery, AI contract checks, PR review, deploy). Skills operationalize `AGENTS.md`; they do not replace it.

Defaults:

- Linear defines requirements; GitHub defines code and PR state.
- Smallest correct change; no unrelated refactors or new dependencies without justification.
- Do not merge, deploy, rotate secrets, or change production config without explicit human approval.
- Backend domain logic lives in services/agents, not controllers.
- AI outputs must comply with `AI_CONTRACT.md` and existing eval/contract checks.

Backend depth: this repo's `AGENTS.md` and `docs/MULTI_AGENT_WORKFLOW.md`.
