name: Backend CI

"on":
  push:
    branches:
      - sec-41-investigate-backend-github-actions-zero-job-workflow

permissions:
  contents: read

jobs:
  lint-test-build:
    name: Lint, test, and build
    runs-on: ubuntu-latest

    steps:
      - name: Confirm backend CI schedules
        run: echo "backend ci schedules"
