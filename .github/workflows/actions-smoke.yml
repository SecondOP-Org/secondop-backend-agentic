name: Actions Smoke Test

"on":
  workflow_dispatch:
  push:
    branches:
      - sec-41-investigate-backend-github-actions-zero-job-workflow

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Confirm runner starts
        run: echo "actions smoke ok"
